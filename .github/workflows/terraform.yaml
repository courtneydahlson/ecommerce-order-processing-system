name: Terraform Deployment
on:
  push:
    branches:
    - dev
jobs:
  terraform-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Print greeting
      run: echo "Hello World!"
    - name: Create files
      run: touch test.py
    - name: Create directory
      run: mkdir testdir
    - name: List files
      run: |
        ls -la
        pwd
